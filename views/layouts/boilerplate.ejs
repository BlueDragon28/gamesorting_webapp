<% if (!htmx.isHTMX) { %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/dist/app.1.3.0.css">
    <script src="/dist/public-js.bundle.1.3.0.js" defer></script>
    <title>GameSorting</title>
</head>
<body class="d-flex flex-column vh-100" data-bs-theme="dark">
<% } %>

    <%- include("../partials/coockiesWarningDialog") %>

    <header class="sticky-top">
        <%- include("../partials/navbar") %> 
    </header>

    <%- include("../partials/runtimeFlashMessage") %>

    <div class="container-fluid overflow-hidden h-100">
        <%- //include("../partials/runtimeFlashMessage") %>

        <!-- Main content of the page is placed there. -->
        <%- body %>
    </div>

    <%- //include("../partials/footer") %> 

    <div id="modal-content-section"></div>

<% if (!htmx.isHTMX) { %>
</body>
</html>
<% } %>
