<% if (!htmx.isHTMX || (htmx.isHTMX && htmx.isBoosted)) { %>
<% layout("layouts/boilerplate.ejs") -%>

<div class="row mt-3">
    <div class="col-12 col-md-6 offset-md-3">
        <div class="card">
            <form class="card-body">
<% } %>

                <h1 class="card-title fs-4">Login</h1>

                <input type="hidden" name="emptySet" value="">

                <div class="form-floating my-3">
                    <input class="form-control" type="text" name="username" id="username" placeholder="" value="">
                    <label for="username">Username/Email</label>
                </div>

                <div class="form-floating my-3">
                    <input class="form-control" type="password" name="password" id="password" placeholder="" value="">
                    <label for="password">Password</label>
                </div>

                <button type="submit" class="btn btn-outline-secondary">Login</button>

<% if (!htmx.isHTMX || (htmx.isHTMX && htmx.isBoosted)) { %>
            </form>
        </div>
    </div>
</div>
<% } %>
