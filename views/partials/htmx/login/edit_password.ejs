<% if (!htmx.isHTMX || (htmx.isHTMX && htmx.isBoosted)) { %>
<% layout("layouts/boilerplate.ejs") -%>

<div class="row mt-3">
    <div class="col-12 col-md-6 offset-md-3">
        <div class="card">
            <form class="card-body">
<% } %>
                <h1 class="card-title fs-4">Update Password</h1>

                <div class="form-floating my-3">
                    <input class="form-control" type="password" name="current-password" id="current-password" placeholder="" value="<%= editValues.currentPassword %>">
                    <label for="current-password">Current Password</label>
                </div>

                <div class="form-floating my-3">
                    <input class="form-control" type="password" name="new-password" id="new-password" placeholder="" value="<%= editValues.newPassword %>">
                    <label for="new-password">New Password</label>
                </div>

                <div class="form-floating my-3">
                    <input class="form-control" type="password" name="new-retyped-password" id="new-retyped-password" placeholder="" value="<%= editValues.newPassword %>">
                    <label for="new-retyped-password">Retyped Password</label>
                </div>

                <button class="btn btn-outline-secondary" type="submit">Update Password</button>

<% if (!htmx.isHTMX || (htmx.isHTMX && htmx.isBoosted)) { %>
            </form>
        </div>
    </div>
</div>
<% } %>
