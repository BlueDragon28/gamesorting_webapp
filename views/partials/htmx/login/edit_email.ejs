<% if (!htmx.isHTMX || (htmx.isHTMX && htmx.isBoosted)) { %>
<% layout("layouts/boilerplate.ejs") -%>

<div class="row mt-3">
    <div class="col-12 col-md-6 offset-md-3">
        <div class="card">
            <form class="card-body">
<% } %>

                <h1 class="card-title fs-4">Update Email</h1>

                <div class="form-floating my-3">
                    <input class="form-control" type="email" name="email" id="email" placeholder="" value="<%= editValues.email %>">
                    <label for="email">Email</label>
                </div>

                <div class="form-floating my-3">
                    <input class="form-control" type="password" name="password" id="password" placeholder="" value="<%= editValues.password %>">
                    <label for="password">Password</label>
                </div>

                <button class="btn btn-outline-secondary" type="submit">Update Email</button>
            
<% if (!htmx.isHTMX || (htmx.isHTMX && htmx.isBoosted)) { %>
            </form>
        </div>
    </div>
</div>
<% } %>
