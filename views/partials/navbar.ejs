<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid">
        <a href="/" class="navbar-brand">GameSorting</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon navbar-dark"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="/collections" class="nav-link <%= activeLink === 'Collections' ? 'active': '' %>">Collections</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                <% if (currentUser) { %>
                    <% if (isUserAdmin) { %>
                        <li class="nav-item">
                            <a href="/admin" class="nav-link">Admin</a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a href="/users/informations" class="nav-link <%= activeLink === 'UserInformations' ? 'active' : '' %>"><%= currentUser.username %></a>
                    </li>
                    <li class="nav-item">
                        <a href="/users/logout" class="nav-link">Logout</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a href="/users/login" class="nav-link <%= activeLink === 'UserLogin' ? 'active' : '' %>">Login</a>
                    </li>
                    <li class="nav-item">
                        <a href="/users/register" class="nav-link <%= activeLink === 'UserRegister' ? 'active' : '' %>">Register</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
